{
  "name": "reshape-loader",
  "description": "Reshape loader for Webpack",
  "version": "0.5.0-1",
  "author": "Jeff Escalante",
  "ava": {
    "verbose": "true",
    "serial": "true"
  },
  "bugs": "https://github.com/reshape/loader/issues",
  "dependencies": {
    "reshape": "^0.4.1",
    "reshape-include": "next"
  },
  "devDependencies": {
    "ava": "^0.19.0",
    "coveralls": "^2.11.15",
    "nyc": "^10.1.2",
    "reshape-custom-elements": "^0.1.0",
    "reshape-expressions": "^0.1.2",
    "snazzy": "^6.0.0",
    "source-loader": "^0.2.0",
    "standard": "^9.0.0",
    "sugarml": "^0.5.0",
    "webpack": "beta",
    "when": "^3.7.7"
  },
  "engine": ">=6",
  "homepage": "https://github.com/reshape/loader",
  "keywords": [
    "html",
    "loader",
    "reshape",
    "webpack"
  ],
  "license": "MIT",
  "main": "lib",
  "repository": "reshape/loader",
  "scripts": {
    "coverage": "nyc ava && nyc report --reporter=html && open ./coverage/index.html",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "lint": "standard | snazzy",
    "pretest": "npm run lint",
    "test": "nyc ava"
  }
}
